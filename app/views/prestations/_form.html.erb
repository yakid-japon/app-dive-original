<%= form_with(model: prestation, local: true) do |form| %>
  <% if prestation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prestation.errors.count, "error") %> prohibited this prestation from being saved:</h2>

      <ul>
      <% prestation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_prest %>
    <%= form.text_field :name_prest %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
  <%= form.collection_select :hospital_id, Hospital.order(:hospi_name),:id,:hospi_name, include_blank: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
